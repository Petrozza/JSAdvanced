function nthElement(arr, n) {
    let result = [];
    for (let i = 0; i < arr.length; i += n) {
        result.push(arr[i]);
    }
    return nthElementArr;
    //console.log(result.join('\r\n'));
}
nthElement(['dsa',
'asd', 
'test', 
'tset'], 
2

);